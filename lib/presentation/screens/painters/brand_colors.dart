import 'package:flutter/material.dart';

/// Gris corporativo (Chevrolet), como segundo color de marca junto al azul
/// seleccionado en `colorsAppTheme`. Es un color fijo, no depende del tema.
const chevroletGray = Color(0xFF54646E);
